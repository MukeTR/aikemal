{
  "name": "aikemal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "concurrently -k -n web,api \"npm run dev -w @aikemal/web\" \"npm run dev -w @aikemal/api\"",
    "build": "npm run build -w @aikemal/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "check": "npm run typecheck && npm test && npm run build",
    "preview": "npm run build && npm run preview -w @aikemal/api",
    "deploy": "npm run build && npm run deploy -w @aikemal/api",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.9.7",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
